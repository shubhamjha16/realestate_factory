# Public by definition. Anything with a VITE_ prefix is compiled into the bundle
# served to strangers — no secret ever gets that prefix.
VITE_API_BASE_URL=http://localhost:8004/api/v1
VITE_MAP_STYLE_URL=
VITE_SENTRY_DSN=
