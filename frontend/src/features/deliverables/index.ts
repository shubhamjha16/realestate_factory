/**
 * `deliverables` feature barrel.
 *
 * Another feature imports from here and nowhere else (§3 boundary rule,
 * lint-enforced). Contents land in S12 — generated documents, versions, provenance view.
 */

export {};
