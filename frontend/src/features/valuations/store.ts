/**
 * Client state for `valuations` — Zustand.
 *
 * Server state is not client state: anything fetched lives in TanStack Query.
 * This holds only what the user is doing right now (selection, filters, drafts).
 */

export {};
