/**
 * `auth` feature barrel.
 *
 * Another feature imports from here and nowhere else (§3 boundary rule,
 * lint-enforced). Contents land in S5 — JWT, Google OAuth, enforced MFA, roles.
 */

export {};
