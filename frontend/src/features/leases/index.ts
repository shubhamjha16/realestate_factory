/**
 * `leases` feature barrel.
 *
 * Another feature imports from here and nowhere else (§3 boundary rule,
 * lint-enforced). Contents land in S16 — lease schedule, escalations, rent roll.
 */

export {};
