/**
 * `usage` feature barrel.
 *
 * Another feature imports from here and nowhere else (§3 boundary rule,
 * lint-enforced). Contents land in S18 — plan quotas, spend against the cost ledger.
 */

export {};
