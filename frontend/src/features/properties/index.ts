/**
 * `properties` feature barrel.
 *
 * Another feature imports from here and nowhere else (§3 boundary rule,
 * lint-enforced). Contents land in S5 — property record, parcels, units, geometry.
 */

export {};
